#!/usr/bin/env bash
# Fast-path checks at commit time. CI remains the guarantee — this exists so
# malformed work never leaves the machine. Activate with:
#   git config core.hooksPath .githooks
#
# TEMPLATE: replace the package blocks with your stack's checks. Keep each
# block cheap (seconds, not minutes) — expensive suites belong in CI.
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"

# --- typecheck (adjust per package) -----------------------------------------
# echo "==> Backend type-check..."
# (cd "$ROOT/backend" && npx tsc --noEmit)
# echo "==> Frontend type-check..."
# (cd "$ROOT/frontend" && npx vue-tsc --noEmit)

# --- lint staged files (optional, fast) -------------------------------------
# STAGED=$(git diff --cached --name-only --diff-filter=ACM | grep -E '\.(ts|tsx|vue|js)$' || true)
# if [[ -n "$STAGED" ]]; then
#   echo "==> Linting staged files..."
#   npx eslint --fix $STAGED && git add $STAGED
# fi

echo "==> Pre-commit checks passed."
